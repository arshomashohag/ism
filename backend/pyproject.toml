[tool.ruff]
line-length = 79
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    # SQLAlchemy relationship() uses string forward refs that ruff cannot resolve
    "F821",
]
